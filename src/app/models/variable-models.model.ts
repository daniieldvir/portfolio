export interface Highlight {
    icon: string;
    title: string;
    description: string;
    stats?: string;
}

export interface PageSection {
    label: string;
    id: string;
}

export interface Question {
    question: string;
    answer: string;
  }

